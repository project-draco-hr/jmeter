{
  running=true;
  long time;
  totalLag=0;
  count=0;
  while (running) {
    time=System.currentTimeMillis();
    try {
      Thread.sleep(incr);
    }
 catch (    InterruptedException e) {
    }
    time=System.currentTimeMillis() - time;
    totalLag+=time - incr;
    count++;
  }
}
