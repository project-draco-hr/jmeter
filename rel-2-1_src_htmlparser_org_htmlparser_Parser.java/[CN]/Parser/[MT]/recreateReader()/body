{
  InputStreamReader in;
  if (null == input)   createReader();
 else {
    input.reset();
    input.mark(Integer.MAX_VALUE);
    in=createInputStreamReader();
    reader=new NodeReader(in,resourceLocn);
    reader.setParser(this);
  }
}
