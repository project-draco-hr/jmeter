{
  System.out.println("Handling " + file.getName());
  int count=0;
  if (file.isDirectory()) {
    count=recurseDirectory(file,count);
  }
 else {
    count=countLinesIn(file);
  }
  return count;
}
