{
  String in=",,,a,bc";
  String out[]=JMeterUtils.split(in,",","");
  assertEquals(4,out.length);
  assertEquals("",out[0]);
  assertEquals("",out[1]);
  assertEquals("a",out[2]);
  assertEquals("bc",out[3]);
}
