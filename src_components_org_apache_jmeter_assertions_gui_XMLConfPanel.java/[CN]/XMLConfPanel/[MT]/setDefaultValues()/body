{
  setWhitespace(false);
  setValidate(false);
  setTolerant(false);
  setNamespace(false);
  quiet.setSelected(true);
  reportErrors.setSelected(false);
  showWarnings.setSelected(false);
  tolerant();
}
