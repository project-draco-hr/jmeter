{
  if (!isPerThread()) {
synchronized (counterLock) {
      increaseIteration();
      if (runThisTime) {
        increaseExecutions();
      }
    }
  }
 else {
    increaseIteration();
    if (runThisTime) {
      increaseExecutions();
    }
  }
  reInitialize();
}
