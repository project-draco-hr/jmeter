{
  log.debug("Start : fireDataChanged1");
  if (bigChange) {
    for (    GraphAccumListener gal : listeners) {
      gal.updateGui();
    }
    bigChange=false;
  }
 else {
    quickUpdate(current);
  }
  log.debug("End : fireDataChanged1");
}
