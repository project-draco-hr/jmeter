{
synchronized (calc) {
    calc.addValue(res.getTime());
    int count=calc.getCount();
    Sample newS=new Sample(res.getSampleLabel(),res.getTime(),0,0,0,0,0,0,res.isSuccessful(),count,res.getTimeStamp(),res.getBytes(),res.getThreadName());
    model.addRow(newS);
  }
  currentData=res.getTime();
  updateTextFields();
}
