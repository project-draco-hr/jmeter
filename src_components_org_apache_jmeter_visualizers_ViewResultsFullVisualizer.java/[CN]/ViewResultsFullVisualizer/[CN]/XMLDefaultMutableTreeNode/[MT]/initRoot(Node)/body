{
  NodeList childNodes=xmlRoot.getChildNodes();
  if (childNodes == null)   initAttributeNode(xmlRoot,this);
  for (int i=0; i < childNodes.getLength(); i++) {
    Node childNode=childNodes.item(i);
    initNode(childNode,this);
  }
}
