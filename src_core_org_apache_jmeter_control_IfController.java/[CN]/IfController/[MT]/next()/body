{
  boolean result=true;
  if (isEvaluateAll() || isFirst()) {
    result=evaluateCondition(getCondition());
  }
  if (result) {
    return super.next();
  }
  try {
    return nextIsNull();
  }
 catch (  NextIsNullException e1) {
    return null;
  }
}
