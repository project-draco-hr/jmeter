{
  for (  Thread t : allThreads.values()) {
    if (t != null) {
      while (t.isAlive()) {
        try {
          t.join(WAIT_TO_DIE);
        }
 catch (        InterruptedException e) {
        }
      }
    }
  }
}
