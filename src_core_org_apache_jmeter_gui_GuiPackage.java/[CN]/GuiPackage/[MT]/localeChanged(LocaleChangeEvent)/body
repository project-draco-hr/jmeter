{
  updateCurrentNode();
  guis=new HashMap();
  nodesToGui=new HashMap();
  testBeanGUIs=new HashMap();
  Introspector.flushCaches();
  getMainFrame().setMainPanel((javax.swing.JComponent)getCurrentGui());
  getMainFrame().setEditMenu(getTreeListener().getCurrentNode().createPopupMenu());
}
