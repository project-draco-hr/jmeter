{
  try {
    JMeterGUIComponent gui=(JMeterGUIComponent)Class.forName(((JComponent)e.getSource()).getName()).newInstance();
    addObjectToTree(gui);
  }
 catch (  Exception err) {
    log.error("",err);
  }
}
