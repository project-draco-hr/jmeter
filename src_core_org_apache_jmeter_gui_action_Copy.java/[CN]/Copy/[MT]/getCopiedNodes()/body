{
  if (copiedNodes == null) {
    return null;
  }
  for (int i=0; i < copiedNodes.length; i++) {
    if (copiedNodes[i] == null) {
      return null;
    }
  }
  return cloneTreeNodes(copiedNodes);
}
