{
  addSeparator(menu);
  menu.add(makeMenuItem(JMeterUtils.getResString("paste"),"Paste",ActionNames.PASTE,KeyStrokes.PASTE));
  menu.add(makeMenuItem(JMeterUtils.getResString("reset_gui"),"Reset",ActionNames.RESET_GUI));
}
