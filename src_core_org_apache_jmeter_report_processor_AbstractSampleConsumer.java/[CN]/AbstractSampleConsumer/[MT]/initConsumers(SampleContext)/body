{
  for (  SampleConsumer consumer : this.sampleConsumers) {
    try {
      consumer.setSampleContext(context);
    }
 catch (    Exception e) {
      log.error("produce(): Consumer failed with message :" + e.getMessage(),e);
      throw new SampleException(e);
    }
  }
}
