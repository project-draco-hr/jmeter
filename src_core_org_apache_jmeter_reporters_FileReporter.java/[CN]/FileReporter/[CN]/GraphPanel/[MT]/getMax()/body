{
  float maxValue=0;
  for (  String key : keys) {
    List<Integer> temp=data.get(key);
    for (    Integer t : temp) {
      float f=t.intValue();
      maxValue=Math.max(f,maxValue);
    }
  }
  return (float)(maxValue + maxValue * 0.1);
}
