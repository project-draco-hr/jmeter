{
synchronized (LOCK) {
    instanceCount--;
    if (instanceCount <= 0) {
      finalizeFileOutput();
      Runtime.getRuntime().removeShutdownHook(shutdownHook);
      inTest=false;
    }
  }
  if (summariser != null) {
    summariser.testEnded(host);
  }
}
