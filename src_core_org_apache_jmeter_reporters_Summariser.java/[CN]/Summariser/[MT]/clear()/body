{
  myName=this.getName();
synchronized (accumulators) {
    Totals tots=(Totals)accumulators.get(myName);
    if (tots != null) {
      tots.clear();
    }
 else {
      tots=new Totals();
      accumulators.put(myName,tots);
    }
  }
  super.clear();
}
