{
  SampleResult res=new SampleResult();
  res.sampleStart();
  Thread.sleep(100);
  res.samplePause();
  Thread.sleep(200);
  res.sampleResume();
  Thread.sleep(100);
  res.sampleEnd();
  assertTrue(res.getTime() >= 200);
  assertFalse(res.getTime() >= 290);
}
