{
  this();
  allThreads=res.allThreads;
  assertionResults=res.assertionResults;
  bytes=res.bytes;
  contentType=res.contentType;
  dataEncoding=res.dataEncoding;
  dataType=res.dataType;
  endTime=res.endTime;
  groupThreads=res.groupThreads;
  idleTime=res.idleTime;
  isMonitor=res.isMonitor;
  label=res.label;
  latency=res.latency;
  location=res.location;
  parent=res.parent;
  pauseTime=res.pauseTime;
  requestHeaders=res.requestHeaders;
  responseCode=res.responseCode;
  responseData=res.responseData;
  responseHeaders=res.responseHeaders;
  responseMessage=res.responseMessage;
  sampleCount=res.sampleCount;
  samplerData=res.samplerData;
  saveConfig=res.saveConfig;
  startTime=res.startTime;
  stopTest=res.stopTest;
  stopTestNow=res.stopTestNow;
  stopThread=res.stopThread;
  subResults=res.subResults;
  success=res.success;
  threadName=res.threadName;
  time=res.time;
  timeStamp=res.timeStamp;
}
