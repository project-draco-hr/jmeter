{
  if (obj instanceof Saveable) {
    try {
      ((SaveHandler)((Saveable)obj).getTagHandlerClass().newInstance()).save((Saveable)obj,out);
    }
 catch (    Exception ex) {
      log.error("",ex);
    }
  }
 else {
    out.write(convertToXML(obj.toString()));
  }
}
