{
  for (int i=0; i < names.length; i++) {
    PropertyDescriptor p=property(names[i]);
    p.setValue("group",group);
    p.setValue("order",new Integer(i));
  }
  numCreatedGroups++;
  getBeanDescriptor().setValue("group." + group + ".order",new Integer(numCreatedGroups));
}
