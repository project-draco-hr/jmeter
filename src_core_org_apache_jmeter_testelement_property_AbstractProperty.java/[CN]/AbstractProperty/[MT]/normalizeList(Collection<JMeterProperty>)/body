{
  Collection<JMeterProperty> newColl=null;
  for (  Object item : coll) {
    if (newColl == null) {
      try {
        @SuppressWarnings("unchecked") final Class<Collection<JMeterProperty>> class1=(Class<Collection<JMeterProperty>>)coll.getClass();
        newColl=class1.newInstance();
      }
 catch (      Exception e) {
        log.error("Bad collection",e);
        return coll;
      }
    }
    newColl.add(convertObject(item));
  }
  if (newColl != null) {
    return newColl;
  }
 else {
    return coll;
  }
}
