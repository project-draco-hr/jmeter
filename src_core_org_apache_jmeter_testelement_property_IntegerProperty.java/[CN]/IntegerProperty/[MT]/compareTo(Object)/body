{
  if (arg0 instanceof JMeterProperty) {
    int argValue=((JMeterProperty)arg0).getIntValue();
    if (value < argValue) {
      return -1;
    }
 else     if (value == argValue) {
      return 0;
    }
 else {
      return 1;
    }
  }
 else {
    return -1;
  }
}
