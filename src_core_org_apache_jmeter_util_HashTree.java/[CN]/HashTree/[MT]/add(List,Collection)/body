{
  if (key == null || key.size() == 0) {
    add(values);
    return;
  }
 else   if (key.size() == 1)   add(key.get(0),values);
 else {
    Object temp=key.remove(0);
    add(temp,key.get(0));
    get(temp).add(key,values);
  }
}
