{
  if (keys.size() == 0)   return list();
 else   if (keys.size() == 1)   return get(keys.first()).list();
 else {
    Object temp=keys.first();
    keys.remove(temp);
    return get(temp).list(keys);
  }
}
