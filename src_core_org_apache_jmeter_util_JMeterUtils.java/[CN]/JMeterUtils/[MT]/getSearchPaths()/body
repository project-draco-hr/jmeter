{
  String[] paths=JMeterUtils.getPropDefault("search_paths",null).split(";");
  String[] result=new String[paths.length + 1];
  result[0]=getJMeterHome() + "/lib/ext";
  for (int i=1; i < result.length; i++) {
    result[i]=paths[i - 1];
  }
  return result;
}
