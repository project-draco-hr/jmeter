{
  if (key.size() == 0) {
    return;
  }
 else   if (key.size() == 1) {
    set(key.get(0),values);
  }
 else {
    Object temp=key.get(0);
    add(temp,key.get(1));
    get(temp).set(key.subList(1,key.size()),values);
  }
}
