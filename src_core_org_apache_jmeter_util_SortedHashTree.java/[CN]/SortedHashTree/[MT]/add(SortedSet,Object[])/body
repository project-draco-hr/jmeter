{
  if (key.size() == 0)   return;
 else   if (key.size() == 1)   add(key.first(),values);
 else {
    Object temp=key.first();
    key.remove(temp);
    add(temp,key.first());
    get(temp).add(key,values);
  }
}
