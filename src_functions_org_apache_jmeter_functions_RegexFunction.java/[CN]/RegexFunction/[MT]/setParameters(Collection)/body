{
  values=parameters.toArray();
  if ((values.length < MIN_PARAMETER_COUNT) || (values.length > MAX_PARAMETER_COUNT)) {
    throw new InvalidVariableException("Parameter Count " + values.length + " not between "+ MIN_PARAMETER_COUNT+ " & "+ MAX_PARAMETER_COUNT);
  }
}
