{
  Collection params=this.parseArguments(parameters);
  String[] values=(String[])params.toArray(new String[0]);
  fileName=values[0];
  if (values.length > 1) {
    myName=values[1];
  }
  openFile();
}
