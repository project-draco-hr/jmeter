{
  List<Object> temp;
  Object o;
  Iterator<String> it=data.keySet().iterator();
  while (it.hasNext()) {
    temp=data.get(it.next());
    o=temp.get(row1);
    temp.set(row1,temp.get(row2));
    temp.set(row2,o);
  }
}
