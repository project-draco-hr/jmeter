{
  String[] contents=getDataAsText();
  StringBuilder sb=new StringBuilder();
  boolean first=true;
  for (  String content : contents) {
    if (!first) {
      sb.append("\n");
    }
 else {
      first=false;
    }
    sb.append(content);
  }
  return sb.toString();
}
