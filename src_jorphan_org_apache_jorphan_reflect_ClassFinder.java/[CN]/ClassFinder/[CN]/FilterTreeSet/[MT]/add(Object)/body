{
  if (contains(o))   return false;
  String s=(String)o;
  if ((s.indexOf("$") == -1) || inner) {
    if (isChildOf(parents,s,contextClassLoader)) {
      return super.add(s);
    }
  }
  return false;
}
