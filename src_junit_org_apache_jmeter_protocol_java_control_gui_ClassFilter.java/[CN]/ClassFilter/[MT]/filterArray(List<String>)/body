{
  ArrayList<Object> newlist=new ArrayList<Object>();
  for (  String item : items) {
    if (include(item)) {
      newlist.add(item);
    }
  }
  if (newlist.size() > 0) {
    return newlist.toArray();
  }
 else {
    return new Object[0];
  }
}
