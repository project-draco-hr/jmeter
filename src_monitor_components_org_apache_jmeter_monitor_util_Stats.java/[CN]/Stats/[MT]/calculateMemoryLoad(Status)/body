{
  double load=0;
  if (stat != null) {
    double total=(double)stat.getJvm().getMemory().getTotal();
    double free=(double)stat.getJvm().getMemory().getFree();
    load=(free / total);
  }
  return (int)(load * 100);
}
