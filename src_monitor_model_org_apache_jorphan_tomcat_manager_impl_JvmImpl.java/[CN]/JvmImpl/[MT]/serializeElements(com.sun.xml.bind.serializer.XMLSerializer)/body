{
  if (_Memory instanceof javax.xml.bind.Element) {
    context.childAsElements(((com.sun.xml.bind.serializer.XMLSerializable)_Memory));
  }
 else {
    context.startElement("","memory");
    context.childAsAttributes(((com.sun.xml.bind.serializer.XMLSerializable)_Memory));
    context.endAttributes();
    context.childAsElements(((com.sun.xml.bind.serializer.XMLSerializable)_Memory));
    context.endElement();
  }
}
