{
  long time=0;
  try {
    Date date=dateFormat.parse(dateStr);
    time=date.getTime();
  }
 catch (  ParseException e) {
    time=0;
    log.error("DateFormat.ParseException: ",e);
  }
  return time;
}
