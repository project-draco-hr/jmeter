{
  super.clearGui();
  getImages.setSelected(false);
  isMon.setSelected(false);
  useMD5.setSelected(false);
  urlConfigGui.clear();
  embeddedRE.setText("");
  if (!isAJP) {
    sourceIpAddr.setText("");
  }
}
