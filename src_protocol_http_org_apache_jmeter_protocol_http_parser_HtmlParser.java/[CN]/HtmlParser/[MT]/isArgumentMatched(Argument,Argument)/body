{
  return (arg.getName().equals(patternArg.getName()) || matcher.matches(arg.getName(),compiler.compile(patternArg.getName()))) && (arg.getValue().equals(patternArg.getValue()) || matcher.matches((String)arg.getValue(),compiler.compile((String)patternArg.getValue())));
}
