{
  if (url == null)   return;
  boolean b=false;
  try {
    b=urls.add(new URL(baseUrl,url));
  }
 catch (  MalformedURLException mfue) {
    b=urls.add(url);
  }
  if (b) {
    log.debug("Added   " + url);
  }
 else {
    log.debug("Skipped " + url);
  }
}
