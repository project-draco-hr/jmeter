{
  if (url == null || url.length() == 0)   return false;
  boolean b=false;
  try {
    b=this.add(new URL(baseUrl,url));
  }
 catch (  MalformedURLException mfue) {
    b=this.add(url);
  }
  return b;
}
