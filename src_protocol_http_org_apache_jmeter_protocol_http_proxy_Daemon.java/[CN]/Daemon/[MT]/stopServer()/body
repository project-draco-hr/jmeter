{
  this.running=false;
  Socket endIt=null;
  try {
    endIt=new Socket(InetAddress.getLocalHost().getHostName(),daemonPort);
    endIt.getOutputStream().write(5);
  }
 catch (  IOException e) {
  }
 finally {
    try {
      endIt.close();
    }
 catch (    Exception e) {
    }
  }
}
