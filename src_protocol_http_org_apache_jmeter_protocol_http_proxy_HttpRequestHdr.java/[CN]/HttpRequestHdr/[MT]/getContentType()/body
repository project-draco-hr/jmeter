{
  String contentType=(String)headers.get("Content-Type");
  if (contentType == null) {
    contentType=(String)headers.get("Content-type");
  }
  if (contentType == null) {
    contentType=(String)headers.get("content-type");
  }
  return contentType;
}
