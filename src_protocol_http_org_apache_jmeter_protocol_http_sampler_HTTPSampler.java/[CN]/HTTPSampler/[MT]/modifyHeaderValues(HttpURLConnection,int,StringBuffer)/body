{
  if ("transfer-encoding".equalsIgnoreCase(conn.getHeaderFieldKey(headerIndex))) {
    return;
  }
  resultBuf.append(conn.getHeaderFieldKey(headerIndex));
  resultBuf.append(": ");
  resultBuf.append(conn.getHeaderField(headerIndex));
  resultBuf.append("\n");
}
