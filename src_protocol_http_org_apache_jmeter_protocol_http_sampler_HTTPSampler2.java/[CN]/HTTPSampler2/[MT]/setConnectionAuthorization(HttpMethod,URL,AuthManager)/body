{
  if (authManager != null) {
    Authorization authHeader=authManager.getAuthForURL(u);
    if (authHeader != null) {
      httpState.setCredentials("realm",authHeader.getURL(),new NTCredentials(authHeader.getUser(),authHeader.getPass(),"thishost","targetdomain"));
    }
  }
}
