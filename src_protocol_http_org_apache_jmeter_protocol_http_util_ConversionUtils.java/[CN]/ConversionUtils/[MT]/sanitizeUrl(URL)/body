{
  URIBuilder builder=new URIBuilder().setScheme(url.getProtocol()).setHost(url.getHost()).setPort(url.getPort()).setUserInfo(url.getUserInfo()).setPath(URLDecoder.decode(url.getPath(),"UTF-8")).setQuery(url.getQuery());
  URI uri=builder.build();
  return uri;
}
