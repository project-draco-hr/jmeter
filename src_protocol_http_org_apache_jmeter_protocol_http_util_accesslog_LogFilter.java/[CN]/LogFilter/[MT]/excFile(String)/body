{
  this.USEFILE=true;
  boolean exc=false;
  for (  String excludeFile : this.EXCFILE) {
    if (text.indexOf(excludeFile) > -1) {
      exc=true;
      this.USEFILE=false;
      break;
    }
  }
  return exc;
}
