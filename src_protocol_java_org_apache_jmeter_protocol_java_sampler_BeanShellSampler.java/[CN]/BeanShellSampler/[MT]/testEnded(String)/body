{
  if (bshInterpreter == null)   return;
  try {
    bshInterpreter.evalNoLog((new StringBuffer("testEnded(")).append(host).append(")").toString());
  }
 catch (  JMeterException ignored) {
    log.debug(ignored.getLocalizedMessage());
  }
}
