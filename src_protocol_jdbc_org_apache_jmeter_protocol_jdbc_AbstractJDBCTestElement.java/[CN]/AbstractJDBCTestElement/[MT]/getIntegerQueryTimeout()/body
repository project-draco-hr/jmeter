{
  int timeout=0;
  try {
    if (StringUtils.isNumeric(query)) {
      timeout=Integer.parseInt(queryTimeout);
    }
  }
 catch (  NumberFormatException nfe) {
    timeout=0;
  }
  return timeout;
}
