{
  super.configure(element);
  connField.setText(element.getProperty(JMeter19ConnectionPool.CONNECTIONS).toString());
  maxUseField.setText(element.getProperty(JMeter19ConnectionPool.MAXUSE).toString());
}
