{
  Iterator<Closeable> itr=clients.iterator();
  while (itr.hasNext()) {
    Closeable client=itr.next();
    try {
      client.close();
    }
 catch (    IOException e) {
    }
    client=null;
  }
  clients.clear();
  client_map.clear();
}
