{
  interrupted=false;
  SUBSCRIBER=new ReceiveSubscriber(getUseJNDIPropertiesAsBoolean(),this.getJNDIInitialContextFactory(),getProviderUrl(),getConnectionFactory(),getDestination(),isUseAuth(),getUsername(),getPassword());
  SUBSCRIBER.resume();
  ClientPool.addClient(SUBSCRIBER);
  log.debug("SubscriberSampler.initReceiveClient called");
}
