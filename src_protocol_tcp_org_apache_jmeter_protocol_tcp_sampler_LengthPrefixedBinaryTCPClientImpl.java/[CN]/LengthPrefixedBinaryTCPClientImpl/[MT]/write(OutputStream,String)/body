{
  try {
    os.write(intToByteArray(s.length() / 2,lengthPrefixLen));
    this.tcpClient.write(os,s);
  }
 catch (  IOException e) {
    log.warn("Write error",e);
  }
  log.debug("Wrote: " + s);
  return;
}
