{
  SampleResult res=new SampleResult();
  res.sampleStart();
  Thread.sleep(100);
  res.sampleEnd();
  long time=res.getTime();
  if (time < 100) {
    fail("Sample time should be >=100, actual " + time);
  }
}
